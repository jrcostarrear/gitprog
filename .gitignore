# pacotes gerados devem ficar dentro do conector
conectores/*/dist/
# compatibilidade: dist legado na raiz
/dist/
*.tar.gz
*.tgz
*.zip
siscconectores/secretos/*.json
!siscconectores/secretos/*.sample.json
siscconectores/token-sisc/*.txt
# token local do kit, nunca entra no pacote
token-sisc/*.txt
.DS_Store
